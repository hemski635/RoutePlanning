# AI Model Configuration
# For GitHub Models (free tier for development)
GITHUB_TOKEN=your_github_personal_access_token
MODEL_ID=openai/gpt-4.1

# For Microsoft Foundry (production)
# AZURE_AI_ENDPOINT=https://your-project.openai.azure.com/
# AZURE_AI_API_KEY=your_azure_api_key

# Routing Configuration
# BRouter (recommended - self-hosted in Docker)
# Start with: docker-compose up -d
BROUTER_URL=http://localhost:17777

# OpenRouteService (optional fallback, requires API key)
# OPENROUTESERVICE_API_KEY=your_openrouteservice_api_key

# Optional: Mapbox for enhanced elevation data
# MAPBOX_TOKEN=your_mapbox_token
